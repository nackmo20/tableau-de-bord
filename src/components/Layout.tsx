import { motion } from 'framer-motion';import { BarChart3, ClipboardList, FileJson, GraduationCap, Megaphone, Rocket } from 'lucide-react';import { switchLegacyTab } from '../services/legacyAdapter';
const tabs=[['intention','Saisie d’intention',ClipboardList],['dashboard','Dashboard global',BarChart3],['deploiement','Déploiement',Rocket],['deploiement-global','Déploiement global',GraduationCap],['communication','Communication',Megaphone]] as const;
export function SidebarNavigation({frame}:{frame:React.RefObject<HTMLIFrameElement|null>}){return <aside className="glass sticky top-4 hidden h-[calc(100vh-2rem)] w-72 shrink-0 rounded-3xl p-4 lg:block"><div className="mb-8 flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-slate-950 text-white"><FileJson size={20}/></div><div><p className="text-sm text-slate-500">OVP</p><h1 className="font-bold text-slate-950">Tableau de bord</h1></div></div><nav className="space-y-2">{tabs.map(([id,label,Icon])=><motion.button key={id} whileHover={{x:3}} onClick={()=>switchLegacyTab(frame.current,id)} className="flex w-full items-center gap-3 rounded-2xl px-3 py-3 text-left text-sm font-medium text-slate-700 hover:bg-slate-100"><Icon size={18}/>{label}</motion.button>)}</nav><p className="mt-8 rounded-2xl bg-blue-50 p-3 text-xs leading-5 text-blue-900">Navigation modernisée façon Aceternity. Les écrans métier originaux restent chargés pour garantir la compatibilité des imports, exports et calculs.</p></aside>}
